mediawiki-xml-analyzer
===============================

This application takes a MediaWiki XML dump and produces some statistics about it. It's then easy to plot those stats in, say, some spreasheet program.
